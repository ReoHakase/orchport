import { defineConfig } from "oxfmt";

export default defineConfig({
  endOfLine: "lf",
  printWidth: 80,
  semi: true,
  singleQuote: false,
  sortImports: true,
  sortPackageJson: true,
  tabWidth: 2,
  trailingComma: "es5",
});
